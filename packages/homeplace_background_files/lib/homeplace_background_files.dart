library;

/// Registers the Android background file bridge with every Flutter engine,
/// including the headless engine created by WorkManager.
final class HomePlaceBackgroundFiles {
  const HomePlaceBackgroundFiles._();
}
